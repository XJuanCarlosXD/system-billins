# Runs log — Plan 2026-06-22-zentoryerp-mcp-foundations.md

## 2026-06-22T (auto-run, Opus 4.7)

Branch: `mcp/foundations` (pushed a origin).

**Steps completados en este run:**
- Task 4 (Context resolver + envelope): pasos 1..6.
- Task 5 (Auth Bearer -> TokenContext): pasos 1..5.
- Task 6 (Audit + rate limit): pasos 1..6. Adaptado: audit usa
  `apps.legacy.client` (no django.db).
- Task 7 (Endpoints admin tokens): pasos 1..6. Adaptado: DRF
  `APIView` + `IsLegacyAdmin` (no JsonResponse / get_current_user).
- Task 8 (Endpoint admin usage): pasos 1..5. Adaptado al mismo
  patron DRF.
- Task 9 (Memory proxy + memoria_* tools): pasos 1..6. Agregado
  `pytest-asyncio>=0.23` a requirements.txt; httpx instalado en
  container running.
- Task 10 (Registry doc types + tools): pasos 1..5.

**Tests:** suite MCP completa pasa en VM:
- test_tokens.py: 4
- test_context_resolver.py: 7
- test_auth.py: 4
- test_ratelimit.py: 4
- test_audit.py: 1
- test_admin_tokens.py: 4
- test_admin_usage.py: 3
- test_memory_proxy.py: 2
- test_doc_types_tools.py: 4
**Total: 33/33 PASS.**

**Commits pushed (este run):** 7
- 0095889 feat(mcp): resolver de contexto cia/punto + envelope uniforme
- 85deeba feat(mcp): auth Bearer -> TokenContext con touch best-effort
- 30f5dfb feat(mcp): audit log + rate limit por token
- 77ac032 feat(mcp): endpoints admin tokens (list/create/revoke/usage)
- aa3da86 feat(mcp): endpoint admin usage con KPIs, serie temporal y tops
- 3141241 feat(mcp): proxy memory-router + tools memoria_*
- d8809e6 feat(mcp): registry tipos de documento + tools doc_tipos_listar/describir

**Proximo step pendiente:** Task 11 Step 1 — Implementar
`apps/mcp/server.py` (montaje del servidor MCP sobre Streamable HTTP
en `/mcp/`). Linea ~1836 del plan.

**Blockers:** 0.

**Notas para el siguiente run:**
- El container backend tiene httpx y pytest-asyncio instalados via pip
  install dentro del container; el rebuild formal con la nueva
  requirements.txt sigue pendiente (Task 14 / sigaft-deploy-vm).
- Task 11 introduce `server.py` con SDK MCP (`mcp.server` + Streamable
  HTTP). Verificar que `mcp>=1.0` ya este disponible en el container
  (NO en pip list actual; tambien require install + posiblemente
  rebuild) antes de correr el smoke server.
- Para Tasks 12 y 13 (frontend admin tokens + usage page), recordar
  patrones de la skill `sigaft-crud-pagination` y `sigaft-ui-facturacion`.

RUN_DONE | steps_done=27 | next_pending=Task 11 Step 1 (server.py) | blockers=0 | commits_pushed=7

---

## Run 2026-06-23 ~14:30 UTC

**Steps completados:**
- Task 12 (UI admin tokens) steps 8, 9 (deferido a Netlify), 10 (commit).
  Resultado: route TanStack `/admin/mcp/tokens` creada; commit
  `feat(mcp): UI admin tokens — lista, crear, revocar, usage drawer`.
- Task 13 (UI usage page) steps 1..11. Componentes:
  `usage-kpis.tsx`, `usage-timeseries.tsx` (recharts),
  `usage-top-tools.tsx`, `usage-top-users.tsx`, `usage-recent-errors.tsx`,
  `mcp-usage-page.tsx`, route `/admin/mcp/usage`.
  Sidebar: entradas `MCP Tokens` y `MCP Usage` agregadas bajo grupo Sistema.
  Step 9 quedo marcado [?]: no existe gating `is_dba` en frontend, las
  entradas son visibles a todo autenticado por ahora. Step 10 (smoke local)
  diferido a Netlify.
- Task 14 (deploy + smoke) step 2: backend ya en sync con VM (md5sum
  identicos en server.py, views_admin.py, views_usage.py, urls.py,
  settings.py, asgi.py, requirements.txt). Container Up, `/mcp/`
  responde 406 sin Accept header (esperado).

**Tests:** no se corrieron tests nuevos este run (todo trabajo frontend;
las reglas indican no usar `npm run build` como validacion).

**Commits pushed (este run):** 2
- bec80fc feat(mcp): UI admin tokens — lista, crear, revocar, usage drawer
- 2632688 feat(mcp): vista de monitoreo /admin/mcp/usage + entradas sidebar

**Steps marcados [?] (necesitan confirmacion humana):**
- Task 13 Step 9: como cablear `is_dba` (campo en /api/profile/, claim,
  flag local). Por ahora las entradas son visibles a todos.
- Task 14 Step 1: push a `main` queda al usuario (regla "NO PR").
- Task 14 Step 3: env vars MEMORY_ROUTER_URL / TOKEN / PROJECT — el
  usuario debe pegar valores reales en `.env` del compose y reiniciar.

**Steps marcados [!] (diferidos a accion humana / post-deploy):**
- Task 12 Step 9, Task 13 Step 10: smoke local frontend — diferido a
  Netlify.
- Task 14 Steps 4..9: requieren login UI, Claude Desktop, smoke con
  llamadas reales — accion del usuario.

**Proximo step pendiente:** todos los steps automatizables estan
cerrados. Lo que queda son acciones humanas (Step 1 push main,
Step 3 env vars, Steps 4..9 smoke con Claude Desktop) + el gate
`is_dba` cuando el usuario decida la fuente del flag.

**Blockers:** 3 confirmaciones humanas pendientes (gate is_dba,
push main, env vars memory-router).

RUN_DONE | steps_done=15 | next_pending=Task 14 Step 1 push main (humano) | blockers=3 | commits_pushed=2

---

## Run 2026-06-23 ~18:00 UTC (auto-trigger 4h)

**Estado del plan al iniciar:** todo lo automatizable cerrado en runs previos. Branch `mcp/foundations` ya en sync con remoto; working tree clean.

**Trabajo realizado este run:**
- Verificacion en VM: `pytest apps/mcp/tests/` -> **35 passed in 1.01s** (5 warnings deprecacion datetime.utcnow, no bloqueantes).
- Endpoint `/mcp/` en container responde (probado tools/list desde dentro del backend container; HTTP 400 = parse de body roto por escapes shell, pero MCP server activo y respondiendo JSON-RPC).
- Limpieza de notas stale: Task 12 Step 10 y Task 13 Step 11 ahora apuntan al hash de commit real (bec80fc y 2632688) en vez de "pendiente de aplicar en este run".
- Self-review checklist al final del plan: marcados 4 items verificables con evidencia (migrate, pytest 35/35, plaintext una vez, revocacion). Los 3 restantes (sidebar gate is_dba, smoke Claude Desktop, smoke /admin/mcp/usage) quedan `[?]`/`[!]` con razon explicita.

**Tests:** 35/35 PASS en VM. Cobertura por archivo:
- test_tokens.py 4, test_context_resolver.py 7, test_auth.py 4,
  test_ratelimit.py 4, test_audit.py 1, test_admin_tokens.py 4,
  test_admin_usage.py 3, test_memory_proxy.py 2, test_doc_types_tools.py 4,
  test_server_smoke.py 2.

**Commits pushed (este run):** 1 (chore actualizando plan + run log; aplicado al final del run).

**Steps pendientes (todos por accion humana):**
- Task 13 Step 9 `[?]`: cablear flag `is_dba` en frontend (campo /api/profile/, claim, flag). Decidir fuente.
- Task 14 Step 1 `[?]`: push a `main` (regla NO PR — usuario lo hace).
- Task 14 Step 3 `[?]`: env vars MEMORY_ROUTER_URL / TOKEN / PROJECT en `.env` del compose VM.
- Task 14 Steps 4..9 `[!]`: smoke con Claude Desktop tras tener token + env vars + frontend live.

**Blockers:** 3 confirmaciones humanas (gate is_dba, push main, env vars memory-router).

RUN_DONE | steps_done=0 (mantenimiento + verificacion) | next_pending=Task 13 Step 9 is_dba (humano) | blockers=3 | commits_pushed=1

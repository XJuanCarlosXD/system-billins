// INV PDF/export helpers — extend as needed with actual jsPDF calls
export function printExistencias(meta: Record<string, unknown>, rows: unknown[]) {
  console.log('printExistencias', meta, rows)
}

export function printMovimientos(meta: Record<string, unknown>, rows: unknown[]) {
  console.log('printMovimientos', meta, rows)
}

export function printKardex(meta: Record<string, unknown>, rows: unknown[]) {
  console.log('printKardex', meta, rows)
}
